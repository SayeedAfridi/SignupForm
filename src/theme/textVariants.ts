import { fp } from '@src/utils/font.utils';

const textVariants = {
  body: {
    fontSize: fp(1.8),
    color: 'text',
  },
  button: {
    fontSize: fp(2),
    color: 'text',
    fontWeight: '700',
  },
  title: {
    fontSize: fp(2),
    color: 'text',
  },
  appbar: {
    fontSize: fp(2),
    color: 'white',
  },
};

export default textVariants;
